// FE-3에서 구현. FE-1은 라우팅 스캐폴딩만 담당한다 (prompts/9-plan.md).
export function LoginPage() {
  return (
    <main style={{ padding: "var(--space-xl)" }}>
      <h1 style={{ fontSize: "var(--fs-headline)", fontWeight: "var(--fw-headline)" }}>로그인</h1>
      <p style={{ color: "var(--ink-muted)" }}>FE-3에서 구현 예정.</p>
    </main>
  );
}

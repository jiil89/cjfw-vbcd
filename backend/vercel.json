{
  "//": "BE-9: 백엔드(Express) 배포 설정. Root Directory=backend로 별도 Vercel 프로젝트를 만드는 경우 이 파일이 적용된다. maxDuration은 5-project-principle.md 5번 'Vercel Hobby 함수 실행시간 최대 300초' 원칙에 따라 CJ 자동화(Playwright 로그인+예약 API 호출)가 시간 안에 끝나도록 넉넉히 잡는다. 프론트(FE-1)가 스캐폴딩되어 같은 Vercel 프로젝트/도메인으로 묶기로 확정되면, 이 파일 대신 모노레포 루트 vercel.json에 프론트 빌드 output과 함께 '/api/*' -> 이 백엔드 함수로의 rewrite를 구성해야 한다(지금은 프론트 디렉터리 구조가 없어 그 rewrite 경로를 미리 확정할 수 없으므로 보류).",
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  }
}
